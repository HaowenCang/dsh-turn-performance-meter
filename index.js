/**
 * Host entry for dsh-turn-performance-meter.
 *
 * The initial scaffold intentionally performs no interception. DeepSeek should
 * implement the host-side telemetry bridge only after validating the exact
 * locally installed DSH event and projection APIs documented in docs/.
 */
export const name = 'dsh-turn-performance-meter'

export function apply() {}
