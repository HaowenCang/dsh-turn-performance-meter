# Client skeleton

The production component belongs in `conversation.composer.dock`. Keep the live pill and completed card in one component tree, but keep their rendering branches separate. Do not scrape another plugin's DOM and do not add another React runtime. See `docs/UI_SPEC.md` and `docs/DSH_API_NOTES.md` before implementation.
